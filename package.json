{
  "name": "snippet.terrierscript.com",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/terrierscript/snippets.terriercript.com",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {
    "lerna": "^3.13.1",
    "netlify-lambda": "^1.4.2"
  },
  "scripts": {
    "build:functions": "netlify-lambda build ./packages/functions",
    "build": "yarn workspace core build && yarn build:functions",
    "serve-function": "yarn dotenv netlify-lambda serve functions",
    "sandbox": "yarn workspace sandbox start"
  }
}
